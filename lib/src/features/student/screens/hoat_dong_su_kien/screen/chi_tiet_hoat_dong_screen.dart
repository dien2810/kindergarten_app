import 'package:flutter/material.dart';

class ChiTietHoatDongScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chi Tiết Hoạt Động')),
      body: Center(
        child: Text('Detailed information about the activity'),
      ),
    );
  }
}
